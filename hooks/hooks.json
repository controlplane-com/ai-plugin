{
  "hooks": {
    "SessionStart": [
      {
        "matcher": "*",
        "hooks": [
          {
            "type": "command",
            "command": "CLAUDE_PLUGIN_ROOT=\"${extensionPath}/plugins/cpln\" bash \"${extensionPath}/plugins/cpln/hooks/inject-rules.sh\""
          }
        ]
      }
    ]
  }
}
